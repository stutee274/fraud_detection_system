[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn app_comp:app --bind 0.0.0.0:5000 --workers 2 --timeout 120"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
PORT = "5000"