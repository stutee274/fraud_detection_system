[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn app_comp:app --bind 0.0.0.0:$PORT --workers 2 --threads 2 --timeout 120 --log-level info"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10